## HAPI v0.25.3.2

- Declares the Web markdown renderer's `react-markdown` import as a direct dependency.
- Removes lock-layout dependence that could surface missing `remark-stringify`, mdast, vfile, and hast types during a clean full typecheck.
- Retains the complete v0.25.3.1 upstream PR and personal-fork feature set.
